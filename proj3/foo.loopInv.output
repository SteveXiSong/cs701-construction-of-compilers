
BASIC BLOCK while.body :DOM-Before: { entry if.end while.cond }  DOM-After: { while.body entry if.end while.cond }
BASIC BLOCK while.cond5 :DOM-Before: { while.body entry if.end while.cond }  DOM-After: { while.body while.cond5 entry if.end while.cond }
BASIC BLOCK while.body7 :DOM-Before: { while.body while.cond5 entry if.end while.cond }  DOM-After: { while.body while.cond5 while.body7 entry if.end while.cond }
BASIC BLOCK while.end :DOM-Before: { while.body while.cond5 entry if.end while.cond }  DOM-After: { while.body while.cond5 while.end entry if.end while.cond }
BASIC BLOCK while.end9 :DOM-Before: { entry if.end while.cond }  DOM-After: { while.end9 entry if.end while.cond }
BASIC BLOCK while.cond10 :DOM-Before: { while.end9 entry if.end while.cond }  DOM-After: { while.end9 while.cond10 entry if.end while.cond }
BASIC BLOCK while.body12 :DOM-Before: { while.end9 while.cond10 entry if.end while.cond }  DOM-After: { while.end9 while.cond10 while.body12 entry if.end while.cond }
BASIC BLOCK if.then15 :DOM-Before: { while.end9 while.cond10 while.body12 entry if.end while.cond }  DOM-After: { while.end9 while.cond10 while.body12 if.then15 entry if.end while.cond }
BASIC BLOCK if.else16 :DOM-Before: { while.end9 while.cond10 while.body12 entry if.end while.cond }  DOM-After: { while.end9 while.cond10 while.body12 if.else16 entry if.end while.cond }
BASIC BLOCK if.end18 :DOM-Before: { while.end9 while.cond10 while.body12 if.else16 entry if.end while.cond }  DOM-After: { while.end9 while.cond10 while.body12 if.else16 if.end18 entry if.end while.cond }
BASIC BLOCK while.end19 :DOM-Before: { while.end9 while.cond10 entry if.end while.cond }  DOM-After: { while.end9 while.cond10 while.end19 entry if.end while.cond }
BASIC BLOCK entry :DOM-Before: { }  DOM-After: { entry }
BASIC BLOCK if.then :DOM-Before: { entry }  DOM-After: { entry if.then }
BASIC BLOCK if.else :DOM-Before: { entry }  DOM-After: { entry if.else }
BASIC BLOCK if.end :DOM-Before: { entry }  DOM-After: { entry if.end }
BASIC BLOCK while.cond :DOM-Before: { entry if.end }  DOM-After: { entry if.end while.cond }


BASIC BLOCK entry :DOM-Before: { }  DOM-After: { entry }
BASIC BLOCK if.then :DOM-Before: { entry }  DOM-After: { entry if.then }
BASIC BLOCK if.else :DOM-Before: { entry }  DOM-After: { entry if.else }
BASIC BLOCK if.end :DOM-Before: { entry }  DOM-After: { entry if.end }


BASIC BLOCK entry :DOM-Before: { }  DOM-After: { entry }
BASIC BLOCK while.cond :DOM-Before: { entry }  DOM-After: { entry while.cond }
BASIC BLOCK while.body :DOM-Before: { entry while.cond }  DOM-After: { entry while.cond while.body }
BASIC BLOCK while.end :DOM-Before: { entry while.cond }  DOM-After: { entry while.cond while.end }

